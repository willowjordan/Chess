# i don't know if i actually need this, i'll come back to it later if it turns out i do
from board import *

class BoardOverlay:
    def __init__(self, board):
        self.board = board

    def draw(self, canvas):
        pass